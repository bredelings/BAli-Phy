This full-program test protects the connection from positional `foreground` annotations through
model-state generation to both default `BranchModel_test` parameters.  The smaller Haskell tests
cover parsing and selection separately; this test can be removed if generated-model tests later
exercise the same state and binding path directly.
