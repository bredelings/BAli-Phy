These full-program cases keep lambda-bound arguments inside the displayed model function.
Successful compilation alone cannot distinguish that from incorrectly extracted parameters.
They are obsolete if model descriptions stop preserving lambda bodies or extraction is removed.
