# The `--tree` command:

-T     <prior>                              Tree prior.
--tree <file>

This selects the prior on the tree.  The implicit parameter `taxa` can
be used to construct the tree.

# Examples:

   bali-phy dna.fasta '--tree=~UniformTree(taxa)'

# See also:

   fix
