immer_inc = include_directories('.', is_system:true)

immer = declare_dependency(include_directories: immer_inc)

install_data('LICENSE',
  install_dir: get_option('datadir') / 'doc' / 'bali-phy' / 'licenses' / 'immer')
